{
    "functions": {
        "pages/**/*": {
            "maxDuration": 60
        }
    },
    "installCommand": "pnpm install &&  npx playwright install-deps chromium && npx playwright install chromium"
}